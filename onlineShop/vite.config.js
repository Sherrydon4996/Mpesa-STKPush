import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
})


// the code you provided for categories are soo big,
// can you first of all reduce it by a half, also reducethe size
// of the container to leave side spaces right and left sides 
// to make the images move like a corosels by themselves not by arrows,
// also the buttons to heat to the categories should be actual buttons
// with text like view category a an arrow icon from react
// icons and the left part should also be equal to button in size